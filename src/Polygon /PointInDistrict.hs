module Polygon.PointInDistrict
    (
        isPointInDistrict
    ) where

import Data.District (District (..), Coords (..))

isPointInDistrict :: Coords -> District -> Bool
isPointInDistrict = undefined
